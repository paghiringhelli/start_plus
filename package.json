{
  "name": "start_plus",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "prebuild": "node scripts/generate-authz-policy.mjs --user-file authorized-nips.local --out public/authz-policy.json",
    "build": "vite build",
    "preview": "vite preview",
    "authz:sign": "node scripts/generate-authz-policy.mjs"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "javascript-obfuscator": "^4.1.1",
    "pngjs": "^7.0.0",
    "sshpk": "^1.18.0",
    "vite": "^8.0.1"
  }
}
